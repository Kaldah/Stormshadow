# Requirements for StormShadow SIP-Only
# Simplified version dependencies

# Configuration and file handling
PyYAML>=6.0

# GUI framework (optional, only needed for GUI mode)
PySide6>=6.5.0

# Network and system utilities (if not using shell scripts)
netifaces>=0.11.0

# Standard library dependencies that might be missing on some systems
asyncio-mqtt>=0.11.0  # For async operations if needed
